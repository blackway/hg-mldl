## [혼자 공부하는 머신러닝+딥러닝] 4강. 훈련 세트와 테스트 세트로 나누어 사용하기

아래 주요 내용: 머신러닝 모델의 **정확한 평가**를 위한 **훈련 세트와 테스트 세트 분리**의 중요성을 설명하고, **넘파이(NumPy)** 라이브러리를 사용하여 데이터를 효율적으로 나누는 방법을 설명함.

**핵심 내용은 다음과 같습니다:**

* **모델 평가의 중요성:**
    * 훈련 데이터로 모델을 훈련하고 같은 데이터로 평가하는 것은 모델의 실제 성능을 제대로 측정할 수 없습니다.
    * 마치 시험문제를 외워서 푸는 것과 같으므로, 실제 성능보다 과대평가될 수 있습니다.
* **훈련 세트와 테스트 세트 분리:**
    * 모델의 일반화 성능을 평가하기 위해 데이터를 훈련 세트와 테스트 세트로 분리해야 합니다.
    * 훈련 세트는 모델 훈련에 사용되고, 테스트 세트는 모델 성능 평가에 사용됩니다.
* **샘플링 편향 문제:**
    * 데이터를 무작위로 나누지 않고 순서대로 나누면 특정 클래스의 데이터가 한쪽에 몰릴 수 있습니다.
    * 이는 모델의 성능을 왜곡하는 샘플링 편향 문제를 일으킵니다.
* **넘파이(NumPy) 활용:**
    * 넘파이는 파이썬의 배열 라이브러리로, 다차원 배열을 효율적으로 처리하고 다양한 수학적 연산을 지원합니다.
    * 넘파이의 `arange()` 함수를 사용하여 인덱스 배열을 생성하고, `shuffle()` 함수로 인덱스를 무작위로 섞습니다.
    * 배열 인덱싱을 통해 훈련 세트와 테스트 세트를 효율적으로 나눌 수 있습니다.
* **데이터 분리 및 모델 평가:**
    * 넘파이를 사용하여 데이터를 무작위로 섞은 후 훈련 세트와 테스트 세트로 나눕니다.
    * 나뉜 데이터를 사용하여 모델을 훈련하고 테스트 세트로 모델의 성능을 평가합니다.
* **검증 데이터:**
    * 테스트 데이터로 모델을 평가 하게 되면 결국 테스트 데이터에 모델이 맞춰져 훈련하게 되는 효과가 생기므로 검증 데이터를 사용해야 합니다.
    * 검증 데이터는 모델을 평가하는 용도로 사용 됩니다.
* **결론:**
    * 머신러닝 모델의 정확한 평가를 위해서는 데이터를 적절히 분리하고 넘파이와 같은 도구를 활용하는 것이 중요합니다.


----


## [혼자 공부하는 머신러닝+딥러닝] 5강. 정교한 결과 도출을 위한 데이터 전처리 알아보기

아래 주요 내용: 머신러닝 모델의 성능 향상을 위한 **데이터 전처리**의 중요성을 설명하고, **넘파이(NumPy)**와 **사이킷런(Scikit-learn)** 라이브러리를 사용하여 데이터를 효율적으로 처리하는 방법을 설명함.

**핵심 내용은 다음과 같습니다:**

* **데이터 전처리의 필요성:**
    * 머신러닝 모델은 입력 데이터의 스케일에 민감하게 반응하며, 스케일이 다른 특성은 모델 성능을 저하시킬 수 있습니다.
    * 특히, K-최근접 이웃(KNN) 알고리즘은 거리 기반 알고리즘이므로 특성 스케일의 영향을 크게 받습니다.
    * 따라서 특성 스케일을 조정하여 모델이 데이터를 공정하게 학습하도록 해야 합니다.
* **표준 점수(Z-점수)를 이용한 스케일 조정:**
    * 표준 점수는 특성의 평균을 빼고 표준 편차로 나누어 스케일을 조정하는 방법입니다.
    * 사이킷런의 `StandardScaler` 클래스를 사용하여 간편하게 표준 점수로 변환할 수 있습니다.
* **넘파이(NumPy)를 이용한 데이터 처리:**
    * 넘파이는 파이썬의 배열 라이브러리로, 다차원 배열을 효율적으로 처리하고 다양한 수학적 연산을 지원합니다.
    * 넘파이의 `column_stack()`, `concatenate()`, `ones()`, `zeros()`, `full()` 함수를 사용하여 데이터를 효율적으로 생성하고 결합할 수 있습니다.
    * 넘파이의 브로드캐스팅 기능을 통해 배열 간의 연산을 효율적으로 수행할 수 있습니다.
* **사이킷런(Scikit-learn)을 이용한 데이터 분할:**
    * 사이킷런의 `train_test_split()` 함수를 사용하여 데이터를 훈련 세트와 테스트 세트로 간편하게 나눌 수 있습니다.
    * `stratify` 매개변수를 사용하여 클래스 비율을 유지하며 데이터를 나눌 수 있습니다.
* **전처리된 데이터를 이용한 모델 훈련 및 평가:**
    * 전처리된 데이터를 사용하여 모델을 훈련하고, 테스트 데이터도 동일한 방식으로 전처리하여 모델을 평가합니다.
    * 훈련 세트의 통계값을 사용하여 테스트 데이터를 변환해야 모델 성능을 제대로 평가할 수 있습니다.
* **결론:**
    * 데이터 전처리는 머신러닝 모델의 성능을 향상시키는 데 필수적인 과정입니다.
    * 넘파이와 사이킷런은 데이터 전처리에 유용한 도구를 제공하며, 효율적인 데이터 처리를 지원합니다.
