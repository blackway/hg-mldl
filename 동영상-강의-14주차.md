---
## 혼자 공부하는 머신러닝 딥러닝 개정판 7장 2절 심층 신경망 강의 요약
심층 신경망은 **입력층, 출력층 외에 하나 이상의 '은닉층'을 추가**한 신경망을 의미합니다.

---
### 은닉층과 활성화 함수

* **은닉층 (Hidden Layer):** 입력층과 출력층 사이에 있는 모든 층을 말합니다.
* **활성화 함수 (Activation Function):** 은닉층의 각 뉴런(유닛)에는 활성화 함수가 추가됩니다.
    * **역할:** 선형적인 계산 결과에 비선형성을 추가하여, 여러 층을 쌓았을 때 각 층이 독립적으로 학습될 수 있도록 합니다. 활성화 함수가 없으면 여러 층이 있어도 결국 하나의 선형 방정식처럼 작동하게 됩니다.
    * **종류:**
        * **소프트맥스 (Softmax):** 일반적으로 다중 분류의 **마지막 출력층**에 사용됩니다. 모든 출력값의 합이 1이 되도록 확률값을 만듭니다.
        * **시그모이드 (Sigmoid):** 초기 은닉층에서 많이 사용되던 활성화 함수입니다. 입력값을 0과 1 사이의 값으로 변환합니다. [Image of Sigmoid function graph]
        * **렐루 (ReLU):** 요즘 가장 널리 사용되는 활성화 함수입니다. 입력값이 0보다 작으면 0을, 0보다 크면 입력값 그대로를 출력하는 매우 간단한 함수입니다. 

---
### 케라스를 이용한 심층 신경망 구현

* **층 추가 방법:** 케라스에서는 `Sequential` 클래스를 사용하여 여러 층을 쌓아 신경망을 만듭니다.
    * **방법 1: 리스트로 한 번에 추가:** `Sequential([Input(), Dense(...), Dense(...)])`
    * **방법 2: `add()` 메소드 사용:** `model = Sequential(); model.add(Input()); model.add(Dense(...))`
* **`Input` 층:** 입력 데이터의 크기를 정의합니다. 실제 층으로 간주되지는 않습니다.
* **`Dense` 층:** 완전 연결 층을 의미하며, 각 유닛(뉴런)의 개수를 지정할 수 있습니다. 예를 들어, `Dense(100)`은 100개의 유닛을 가진 층을 만듭니다. 유닛의 개수는 '하이퍼파라미터'로, 여러 값을 시도하며 최적의 값을 찾아야 합니다.
* **`Flatten` 층:** 입력 데이터를 1차원 배열로 펼쳐주는 역할을 합니다. 예를 들어, 28x28 이미지 데이터를 784개의 특성으로 펼쳐줍니다. 이 층은 모델 파라미터가 없습니다.

---
### 모델 요약 및 파라미터 이해

* **`model.summary()`:** 모델의 각 층 정보, 출력 크기, 파라미터(가중치와 절편) 개수 등을 요약하여 보여줍니다.
    * **출력 크기 (Output Shape):** 첫 번째 차원에 `None`이 붙는 것은 미니 배치 경사 하강법 때문입니다. 모델 훈련 시 배치 크기가 가변적이기 때문에 첫 번째 차원(배치 차원)은 아직 결정되지 않았다는 의미입니다.
    * **파라미터 개수 (Param #):**
        * **가중치 (Weight):** 이전 층의 유닛 수와 현재 층의 유닛 수를 곱한 값입니다. (예: 784개 입력 x 100개 유닛 = 78,400개)
        * **절편 (Bias):** 현재 층의 유닛 수와 같습니다. (예: 100개 유닛 = 100개 절편)
        * 총 파라미터 = 가중치 + 절편

---
### 옵티마이저 (Optimizer)

* **역할:** 모델 파라미터(가중치와 절편)를 업데이트하는 방법을 결정하는 알고리즘입니다. 손실 함수를 최소화하는 방향으로 파라미터를 조정합니다.
* **종류:**
    * **SGD (Stochastic Gradient Descent):** 가장 기본적인 경사 하강법입니다.
        * **학습률 (Learning Rate):** 파라미터를 업데이트하는 속도를 조절합니다. 이 값이 너무 크면 최적점을 지나칠 수 있고, 너무 작으면 학습 속도가 느려집니다.
        * **모멘텀 (Momentum):** 이전 단계의 기울기를 가속도처럼 사용하여 최적점에 더 빠르게 도달하도록 돕습니다.
        * **네스테로프 (Nesterov):** 모멘텀을 개선한 방식입니다.
    * **적응적 학습률 옵티마이저:** 학습률을 자동으로 조절하여 학습 초반에는 빠르게, 최적점에 가까워질수록 느리게 학습합니다.
        * **RMSprop:** 케라스의 기본 옵티마이저입니다.
        * **Adam:** 널리 사용되는 적응적 학습률 옵티마이저 중 하나입니다.

