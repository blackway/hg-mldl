## 혼자 공부하는 머신러닝 딥러닝 개정판 5장 2절 강의 요약

이번 강의는 **교차 검증**과 **그리드 서치**를 통해 머신러닝 모델의 성능을 더 정확하게 평가하고 최적의 하이퍼파라미터를 찾는 방법을 다룹니다.

---

### 1. 테스트 세트의 중요성과 검증 세트 도입
모델의 성능을 평가할 때 **테스트 세트**는 **마지막에 단 한 번만 사용**해야 합니다. 만약 테스트 세트로 여러 번 모델을 평가하고 하이퍼파라미터를 조정한다면, 모델이 테스트 세트에 과적합되어 실제 서비스 환경에서의 성능 예측이 어려워지기 때문입니다. 이를 해결하기 위해 **검증 세트(Validation Set)** 를 도입하여 데이터를 훈련 세트, 검증 세트, 테스트 세트로 나눕니다 (일반적으로 6:2:2 비율). 훈련 세트로 모델을 학습하고, 검증 세트로 하이퍼파라미터를 튜닝하며, 마지막에 테스트 세트로 최종 성능을 평가합니다.

---

### 2. 교차 검증 (Cross-Validation)
데이터셋의 크기가 작아 훈련 세트만으로 모델을 충분히 학습시키기 어렵거나, 검증 세트를 한 번만 나누는 것으로는 모델의 안정적인 성능을 평가하기 어려울 때 **교차 검증**을 사용합니다.

* **원리:** 훈련 데이터(테스트 세트를 제외한 나머지 데이터)를 여러 개의 폴드(Fold)로 나눕니다. 이 중 일부 폴드를 훈련에 사용하고, 나머지 한 폴드를 검증에 사용하여 모델을 여러 번 평가합니다. 각 폴드의 검증 점수를 평균하여 최종 모델의 성능을 측정합니다.
* **장점:** 모든 데이터를 훈련과 검증에 활용하여 모델을 더 안정적으로 평가할 수 있습니다.
* **사이킷런의 `cross_validate()` 함수:** 교차 검증을 쉽게 수행할 수 있도록 지원하며, 기본적으로 5개의 폴드(5-Fold Cross-Validation)로 나눕니다. 분류 모델의 경우 `StratifiedKFold`를 사용하여 각 폴드에 클래스 비율이 균등하게 분배되도록 합니다.

---

### 3. 하이퍼파라미터 튜닝과 그리드 서치
모델의 성능을 최적화하기 위해 사용자가 직접 설정해야 하는 값들을 **하이퍼파라미터**라고 합니다 (예: 결정 트리의 `max_depth`, `min_impurity_decrease`). 여러 하이퍼파라미터의 최적 조합을 찾기 위해서는 체계적인 탐색이 필요합니다.

* **그리드 서치 (Grid Search):**
    * 탐색하고자 하는 하이퍼파라미터 값들의 후보 목록을 미리 지정합니다.
    * 지정된 모든 하이퍼파라미터 조합에 대해 교차 검증을 수행하여 가장 좋은 성능을 보이는 조합을 찾습니다.
    * **사이킷런의 `GridSearchCV`:** 그리드 서치를 간편하게 수행할 수 있는 클래스입니다. `n_jobs=-1`을 설정하면 사용 가능한 모든 CPU 코어를 활용하여 병렬 처리 속도를 높일 수 있습니다.
    * **단점:** 탐색할 하이퍼파라미터의 개수와 각 하이퍼파라미터의 후보 개수가 늘어날수록 조합의 수가 기하급수적으로 증가하여 계산 비용이 매우 커집니다.

---

### 4. 랜덤 서치 (Randomized Search)
그리드 서치의 계산 비용 문제를 보완하기 위해 **랜덤 서치**를 사용합니다.

* **원리:** 하이퍼파라미터의 특정 범위를 지정하고, 그 범위 내에서 무작위로 하이퍼파라미터 조합을 샘플링하여 교차 검증을 수행합니다.
* **사이킷런의 `RandomizedSearchCV`:** 랜덤 서치를 지원하는 클래스입니다.
* **`n_iter` 매개변수:** 샘플링할 하이퍼파라미터 조합의 개수를 지정하여 전체 탐색 횟수를 제어할 수 있습니다. 이를 통해 계산 자원과 시간을 효율적으로 관리할 수 있습니다.
* **확률 분포:** `scipy.stats` 모듈의 `uniform` (실수 균등 분포)이나 `randint` (정수 균등 분포)와 같은 함수를 사용하여 하이퍼파라미터의 샘플링 분포를 정의할 수 있습니다.

**핵심 요약:** 교차 검증과 그리드 서치, 랜덤 서치와 같은 기법들은 모델의 성능을 객관적으로 평가하고 최적의 하이퍼파라미터를 찾는 데 필수적입니다.
