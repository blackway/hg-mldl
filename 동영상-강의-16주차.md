---

# 혼자 공부하는 머신러닝 김러닝 개정판 8장 1절: 20강. 합성곱 신경망의 구성 요소 요약.

## 합성곱 신경망(CNN)의 기본 개념

이 강의는 **합성곱 신경망(Convolutional Neural Network, CNN)**에 대해 설명하며, 특히 기존 인공신경망과 다른 새로운 용어와 개념에 초점을 맞춥니다. CNN은 주로 **컴퓨터 비전(이미지 처리)** 분야에 특화된 신경망입니다.

### 1. 밀집층과 합성곱 연산의 차이
* **밀집층(Dense Layer)**: 입력 특성 전체에 가중치를 곱하고 절편을 더해서 하나의 출력을 만듭니다. 입력이 10개면 가중치도 10개가 필요합니다.
* **합성곱 연산(Convolution Operation)**: 입력 특성의 **일부**에만 가중치를 곱하고, 이 가중치 묶음이 입력 위를 이동(슬라이딩)하면서 여러 개의 출력을 만듭니다. 이렇게 하면 필요한 가중치 개수가 훨씬 줄어들어 효율적입니다.

### 2. 새로운 용어: 커널과 필터
* **커널(Kernel) / 필터(Filter)**: 합성곱 신경망에서 가중치 묶음을 부르는 특별한 이름입니다. 밀집층의 '가중치'와 같은 역할을 하지만, 합성곱 연산에서는 입력 위를 이동하며 특징을 감지하는 역할을 합니다.
    * 보통 **필터**는 여러 개의 커널 묶음을 통칭할 때 사용하며, **커널**은 필터 내의 실제 가중치들을 지칭할 때 사용됩니다. 필터는 여러 개를 사용하여 다양한 특징(예: 색상, 모양, 대각선 등)을 감지하도록 훈련됩니다.

### 3. 특성 맵(Feature Map)
* **특성 맵**: 합성곱 연산을 통해 만들어지는 출력 결과를 말합니다. 입력이 2차원이면 출력(특성 맵)도 2차원 배열로 나옵니다.
* 필터를 여러 개 사용하면 필터 개수만큼 여러 개의 특성 맵이 만들어지고, 이 특성 맵들이 깊이 방향으로 쌓여서 최종 출력을 형성합니다. (예: 필터가 3개면 $2 \times 2 \times 3$ 크기의 특성 맵)

### 4. 패딩(Padding)
* **패딩**: 합성곱 연산 시 입력 이미지의 가장자리에 0 값을 추가하여 가상의 픽셀을 덧붙이는 것을 말합니다.
    * 주로 **제로 패딩(Zero Padding)**을 사용하며, 이는 출력 특성 맵의 크기를 입력 이미지와 동일하게 유지하는 데 사용됩니다. (예: $4 \times 4$ 입력에 $3 \times 3$ 커널을 적용하면 패딩 없을 땐 $2 \times 2$ 출력이지만, 패딩을 하면 $4 \times 4$ 출력이 나옵니다.)
    * 케라스(Keras) 같은 라이브러리에서는 입력과 출력 크기를 같게 해달라고 지정하면 자동으로 필요한 패딩을 추가해줍니다.

### 5. 스트라이드(Stride)
* **스트라이드**: 필터가 입력 위를 이동할 때 몇 칸씩 건너뛸지를 결정하는 값입니다. 기본값은 1칸 이동(한 칸씩)이며, 스트라이드를 늘리면 출력 특성 맵의 크기가 줄어듭니다.
    * 일반적으로 스트라이드는 1을 주로 사용합니다.

### 6. 풀링층(Pooling Layer)
* **풀링**: 만들어진 특성 맵의 크기(높이, 너비)를 줄이는 데 사용되는 연산입니다. 스트라이드를 늘리는 방법과 유사하게 크기를 줄이지만, 풀링층은 합성곱층과 별개의 층으로 존재합니다.
    * **평균 풀링(Average Pooling)**: 특정 영역(예: $2 \times 2$) 안의 모든 값의 평균을 취합니다.
    * **최대 풀링(Max Pooling)**: 특정 영역(예: $2 \times 2$) 안의 값들 중 최댓값을 뽑습니다. 일반적으로 최대 풀링이 패턴 감지에 더 효과적이라고 알려져 있습니다.
    * 풀링층에는 가중치가 없습니다. 풀링은 보통 풀링 사이즈와 스트라이드 사이즈가 같습니다. (예: $2 \times 2$ 풀링이면 두 칸씩 건너뛰며 적용)

### 7. 합성곱 신경망의 일반적인 구조
1.  **입력층**: 이미지 같은 2차원 또는 3차원 데이터가 들어옵니다. (컬러 이미지는 깊이/채널이 3, 흑백 이미지는 1)
2.  **합성곱층(Convolutional Layer)**: 입력에 커널(필터)을 적용하여 특성 맵을 생성합니다. 활성화 함수(주로 ReLU 계열)를 적용하여 비선형성을 추가합니다.
3.  **풀링층(Pooling Layer)**: 특성 맵의 크기를 줄여 모델의 복잡도를 줄이고 중요한 특징을 강조합니다.
4.  **밀집층(Dense Layer)**: 마지막 단계에서 합성곱층과 풀링층을 거쳐 나온 3차원 특성 맵을 1차원으로 펼친 후, 예측을 위해 사용되는 일반적인 신경망 층입니다. (예: 이미지 분류를 위한 클래스 예측)

이러한 요소들이 결합되어 합성곱 신경망은 이미지의 다양한 시각적 특징을 효과적으로 학습하고 분류.
