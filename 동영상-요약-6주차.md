## [혼자 공부하는 머신러닝+딥러닝] 10강, 4장 2절 강의 요약

이번 강의에서는 머신러닝 모델 훈련의 보편적인 방법인 **확률적 경사 하강법 (Stochastic Gradient Descent, SGD)** 에 대해 알아봅니다. 특히 데이터가 점진적으로 추가되는 상황에서의 온라인 학습 방법을 중심으로 설명합니다.

**주요 내용:**

* **확률적 경사 하강법 (SGD)의 개요:**
    * 분류 모델과 회귀 모델 모두에 적용 가능한 범용적인 최적화 방법입니다.
    * 수학적으로 해를 구하기 어렵거나 데이터가 많은 문제에 유용합니다.
    * 딥러닝에서 핵심적인 학습 알고리즘입니다.
* **온라인 학습 (점진적 학습):**
    * 데이터가 한 번에 주어지지 않고 순차적으로 도착하는 상황에서 기존 모델을 재훈련하는 방식입니다.
    * 새로운 데이터가 추가될 때마다 모델 전체를 다시 훈련하는 대신, 기존 모델을 조금씩 업데이트하여 효율성을 높입니다.
    * 확률적 경사 하강법은 온라인 학습에 적합한 방법 중 하나입니다.
* **경사 하강법의 원리:**
    * 모델의 **손실 함수 (Loss Function)** 를 정의하고, 이 손실 함수의 값을 최소화하는 방향으로 모델의 파라미터 (가중치, 계수)를 조금씩 업데이트합니다.
    * **확률적:** 훈련 데이터에서 **하나의 샘플을 무작위로** 선택하여 손실 함수의 기울기를 계산하고 모델을 업데이트합니다.
    * **미니배치 경사 하강법:** 여러 개의 샘플을 묶어서 (미니배치) 기울기를 계산하는 방법입니다 (딥러닝에서 주로 사용).
    * **배치 경사 하강법:** 전체 훈련 데이터를 사용하여 기울기를 계산하는 방법입니다.
* **손실 함수 (Loss Function):**
    * 모델의 예측이 얼마나 틀렸는지 나타내는 지표입니다.
    * 회귀 문제에서는 평균 제곱 오차 등을 사용할 수 있습니다.
    * 분류 문제에서는 **정확도를 손실 함수로 직접 사용하기 어렵습니다.** 정확도는 불연속적인 값이기 때문에 경사 하강법에 적합하지 않습니다.
    * 분류 문제에서는 정확도를 높이는 것과 유사한 효과를 내는 **대리 손실 함수 (Surrogate Loss Function)** 를 사용합니다.
* **로지스틱 손실 함수 (Logistic Loss Function) / 이진 크로스 엔트로피 손실 함수:**
    * 로지스틱 회귀에서 사용되는 손실 함수입니다.
    * 모델의 예측 확률과 실제 타겟 값을 이용하여 손실을 계산합니다.
    * 예측이 정확할수록 손실이 낮아지고, 부정확할수록 손실이 높아지도록 설계됩니다.
* **SGDClassifier:**
    * 사이킷런에서 확률적 경사 하강법을 사용하는 분류 모델 클래스입니다.
    * `loss` 매개변수를 `'log'`로 설정하면 로지스틱 회귀 모델을 훈련합니다.
    * `max_iter` 매개변수로 훈련 에포크 횟수를 지정합니다.
    * **`partial_fit()` 메소드:** 이미 훈련된 모델을 추가적인 데이터로 조금씩 더 훈련할 때 사용합니다 (온라인 학습 지원).
* **에포크와 과대/과소적합:**
    * **에포크 (Epoch):** 전체 훈련 데이터를 한 번 사용하는 훈련 과정의 단위입니다.
    * 확률적 경사 하강법에서는 에포크 횟수에 따라 모델의 과소적합 또는 과대적합 정도가 달라질 수 있습니다.
    * 에포크 횟수가 너무 적으면 과소적합될 수 있고, 너무 많으면 과대적합될 수 있습니다.
    * 학습 곡선을 통해 적절한 에포크 횟수를 찾을 수 있습니다.

**핵심 요약:** 확률적 경사 하강법은 머신러닝 모델을 훈련하는 중요한 최적화 알고리즘이며, 특히 온라인 학습이나 대규모 데이터셋에서 효율적입니다. 분류 문제에서는 정확도 대신 로지스틱 손실 함수와 같은 대리 손실 함수를 사용하여 모델을 훈련하며, 에포크 횟수를 조절하여 과대적합과 과소적합을 방지하는 것이 중요합니다.
