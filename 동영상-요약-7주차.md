## 혼자 공부하는 머신러닝 딥러닝 개정판 5장 1절 강의 요약

[혼자 공부하는 머신러닝+딥러닝] 5장에서는 딥러닝을 제외한 전통적인 머신러닝 알고리즘 중 가장 중요한 알고리즘의 기초인 **결정 트리 (Decision Tree)** 에 대해 설명합니다. 결정 트리는 앙상블 모델의 기반이 되기 때문에 매우 중요한 개념입니다. 이번 시간에는 와인 데이터셋(레드 와인과 화이트 와인 분류 문제)을 사용하여 결정 트리의 기본 개념과 장점을 알아봅니다.

**주요 내용:**

* **와인 데이터셋 분류 문제:** 레드 와인과 화이트 와인의 여러 속성(당도, 알코올 도수, pH 등)을 기반으로 와인의 종류를 예측하는 문제입니다.
* **로지스틱 회귀 모델과의 비교:** 결정 트리의 장점을 부각하기 위해 이전에 학습한 로지스틱 회귀 모델을 동일한 데이터셋에 적용해 봅니다. 로지스틱 회귀 모델은 예측에 사용된 계수의 의미를 직관적으로 이해하기 어렵다는 단점이 있습니다.
* **결정 트리 모델의 소개:**
    * 사이킷런의 `DecisionTreeClassifier` 클래스를 사용하여 분류 결정 트리 모델을 학습합니다.
    * 결정 트리는 **플로차트와 유사한 구조**로, 이해하기 쉬운 모델을 만들 수 있다는 장점이 있습니다.
    * 루트 노드에서 시작하여 특정 특성의 값에 따라 가지를 따라 내려가며 최종적으로 리프 노드에서 클래스를 예측합니다.
    * 각 노드는 특정 특성에 대한 질문을 던지고, 그 질문의 답에 따라 샘플이 두 개의 자식 노드로 분기됩니다 (이진 트리).
* **결정 트리 시각화:** 사이킷런의 `plot_tree()` 함수를 사용하여 학습된 결정 트리 구조를 시각적으로 확인할 수 있습니다. 이를 통해 모델이 어떤 기준으로 데이터를 분류하는지 직관적으로 이해할 수 있습니다.
* **불순도 (Impurity)와 정보 이득 (Information Gain):**
    * 결정 트리는 노드를 분할할 때 **불순도를 최소화**하거나 **정보 이득을 최대화**하는 방향으로 특성과 분할 기준값을 선택합니다.
    * **지니 불순도 (Gini Impurity):** 클래스 분포의 불균형 정도를 나타내는 지표입니다. 특정 노드에 하나의 클래스만 존재할 때 지니 불순도는 0이 됩니다.
    * 결정 트리는 부모 노드의 불순도와 자식 노드의 불순도의 차이가 크도록 (정보 이득이 크도록) 노드를 분할합니다.
* **가지치기 (Pruning):**
    * 결정 트리가 완벽하게 순수한 노드 (하나의 클래스만 포함하는 노드)가 될 때까지 분할하면 훈련 데이터에 과대적합될 가능성이 있습니다.
    * **가지치기**는 트리의 성장을 적절한 수준에서 멈추게 하여 과대적합을 방지하는 방법입니다.
    * `max_depth` 매개변수를 사용하여 트리의 최대 깊이를 제한하는 것이 대표적인 가지치기 방법입니다.
* **특성 스케일링 불필요:** 결정 트리는 각 노드에서 하나의 특성만을 사용하여 분할하기 때문에 **특성 간의 스케일을 맞출 필요가 없습니다.** 이는 데이터 전처리 부담을 줄여주는 큰 장점입니다.
* **특성 중요도 (Feature Importance):**
    * 결정 트리는 어떤 특성이 노드 분할에 많이 사용되었고, 불순도 감소에 큰 영향을 미쳤는지 분석하여 각 특성의 중요도를 계산할 수 있습니다.
    * `feature_importances_` 속성을 통해 특성 중요도 값을 확인할 수 있으며, 이를 통해 데이터 분석에 유용한 정보를 얻을 수 있습니다.

**핵심 요약:** 결정 트리는 이해하기 쉬운 구조, 특성 스케일링 불필요, 특성 중요도 파악 가능 등의 장점을 가진 강력한 머신러닝 알고리즘입니다. 하지만 과대적합될 수 있으므로 적절한 가지치기 전략이 필요합니다. 결정 트리는 앙상블 모델의 기본적인 구성 요소로서 중요한 역할을 합니다.
