## 혼자 공부하는 머신러닝 딥러닝 개정판 5장 3절 강의 요약

이번 강의는 혼자 공부하는 머신러닝 딥러닝 개정판 5장 3절, **트리의 앙상블**에 대한 내용입니다. 이 장에서는 정형 데이터 처리의 핵심인 앙상블 알고리즘, 특히 트리 모델 기반의 앙상블 기법들을 소개합니다.

### 정형 데이터와 비정형 데이터

* **정형 데이터:** CSV, 엑셀 파일처럼 행과 열로 정리된 표 형태의 데이터입니다. 지금까지 배운 선형 회귀, K-최근접 이웃, 로지스틱 회귀, 결정 트리 등이 정형 데이터 처리에 적합한 전통적인 머신러닝 모델입니다.
* **비정형 데이터:** 이미지, 텍스트, 사운드처럼 명확한 형태가 없는 데이터입니다. 딥러닝(신경망) 알고리즘이 주로 비정형 데이터 처리에 사용됩니다.

### 앙상블 알고리즘의 중요성

* **정형 데이터 처리의 최강자:** 앙상블 알고리즘은 정형 데이터 처리에서 가장 뛰어난 성능을 보이며, 캐글(Kaggle)과 같은 데이터 과학 대회에서 최고의 성능을 내기 위해 주로 사용됩니다.

### 대표적인 앙상블 알고리즘

강의에서는 다음 세 가지 주요 앙상블 알고리즘을 설명했습니다.

1.  **랜덤 포레스트 (Random Forest):**
    * **개념:** 100개 이상의 다수의 결정 트리를 무작위로 생성하여 예측을 앙상블(결합)하는 방식입니다. 이름처럼 '숲(Forest)'을 이루는 '나무(Tree)'들이 랜덤하게(Random) 만들어집니다.
    * **동작 원리:**
        * **부트스트랩 샘플링:** 원본 훈련 세트에서 중복을 허용하여 동일한 크기의 가상 샘플 집합(부트스트랩 샘플)을 여러 개 만듭니다.
        * **랜덤 특성 선택:** 각 트리를 만들 때 전체 특성 중 일부(일반적으로 특성 개수의 제곱근)를 무작위로 선택하여 최적의 분할을 찾습니다.
        * **앙상블 예측:** 분류는 각 트리의 확률을 평균하여 가장 높은 확률의 클래스를 선택하고, 회귀는 트리의 출력을 평균합니다.
    * **장점:** 개별 트리의 성능이 낮더라도 여러 트리를 결합하여 높은 일반화 성능을 얻습니다. `OOB (Out-Of-Bag) 샘플`을 활용하여 검증 세트 없이 모델을 평가할 수 있는 장점도 있습니다. `feature_importances_` 속성을 통해 특성 중요도를 확인할 수 있습니다.

2.  **엑스트라 트리 (Extra Trees):**
    * **개념:** 랜덤 포레스트와 유사하게 다수의 트리를 앙상블하지만, 트리를 만들 때 무작위성을 더 주입합니다.
    * **동작 원리:**
        * **전체 샘플 사용:** 부트스트랩 샘플링 없이 전체 훈련 샘플을 사용합니다.
        * **랜덤 분할:** 노드를 분할할 때 최적의 분할 기준을 찾는 대신, 무작위로 특성의 임계값을 선택한 후 그중 최적의 분할을 선택합니다.
    * **장점:** 랜덤 포레스트보다 트리를 더 빠르게 생성할 수 있습니다. `feature_importances_` 속성을 제공합니다.

3.  **그레디언트 부스팅 (Gradient Boosting):**
    * **개념:** 손실 함수(오차)를 줄이는 방향으로 약한 학습기(트리)를 점진적으로 추가하면서 모델을 훈련하는 방식입니다.
    * **동작 원리:** 이전 트리의 예측 오차를 보완하는 방향으로 다음 트리를 만듭니다. 이 과정은 순차적으로 진행되므로 병렬 실행이 어렵습니다.
    * **장점:** 기본 매개변수에서도 과대적합이 덜하며 높은 성능을 얻을 수 있습니다. `learning_rate`와 `n_estimators`를 조절하여 성능을 최적화할 수 있습니다. `feature_importances_` 속성을 제공합니다.

4.  **히스토그램 기반 그레디언트 부스팅 (HistGradientBoostingClassifier):**
    * **개념:** 그레디언트 부스팅의 변종으로, 특성값을 구간(히스토그램 빈)으로 나눠 모델 훈련 속도를 높입니다.
    * **장점:** 빠른 훈련 속도와 높은 일반화 성능을 제공합니다.
    * **특성:** `feature_importances_` 속성은 제공하지 않지만, **`Permutation Importance`**를 사용하여 특성 중요도를 계산할 수 있습니다. Permutation Importance는 특정 특성을 무작위로 섞었을 때 모델 성능이 얼마나 나빠지는지를 측정하여 중요도를 파악합니다.

### 외부 앙상블 라이브러리

* **XGBoost (XGBClassifier)와 LightGBM (LGBMClassifier):** 그레디언트 부스팅의 대표적인 전문 패키지로, 사이킷런의 `HistGradientBoostingClassifier`에 영향을 주었으며 매우 높은 성능을 제공합니다. Colab에 설치되어 있어 쉽게 사용할 수 있습니다.

**결론적으로, 앙상블 알고리즘은 정형 데이터에서 탁월한 성능을 발휘하며, 랜덤 포레스트, 엑스트라 트리, 그레디언트 부스팅, 히스토그램 기반 그레디언트 부스팅 등 다양한 기법이 존재합니다. 이러한 앙상블 기법들은 개별 모델의 약점을 보완하고, 더 강력하고 안정적인 예측 모델을 만드는 데 기여합니다.**

---
다음 시간에는 지도 학습을 넘어 **비지도 학습**에 대해 알아볼 예정입니다.
