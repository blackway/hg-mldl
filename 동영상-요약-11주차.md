---

## 혼자 공부하는 머신러닝 딥러닝 개정판 6장 2절 강의 요약

K-평균 군집 6장 2절에서는 **K-평균(K-Means)** 알고리즘에 대해 설명합니다. 이 알고리즘은 **군집(클러스터링)**의 한 종류로, 미리 정해진 답 없이 데이터를 자동으로 묶어주는 **비지도 학습** 방식입니다.

**K-평균 알고리즘 작동 방식:**

1.  **초기 중심점 설정:** 이미지 픽셀 평균을 구하는 것처럼, 데이터 샘플 중에서 임의로 **K개(클러스터 개수)**의 중심점(센트로이드)을 정합니다.
2.  **클러스터 할당:** 각 샘플과 가장 가까운 중심점을 기준으로 클러스터를 묶습니다.
3.  **중심점 재조정:** 새로 묶인 클러스터의 평균(새로운 중심점)을 다시 계산하여 중심점을 이동시킵니다.
4.  **반복:** 중심점 이동이 더 이상 없거나 클러스터 구성에 변화가 없을 때까지 2, 3번 과정을 반복합니다.

**K-평균 알고리즘의 장점과 단점:**

* **장점:** 간단하면서도 효과적이며 널리 사용됩니다. 새로운 샘플이 주어졌을 때 어느 클러스터에 속하는지 예측하거나(predict), 클러스터 중심까지의 거리를 이용하여 다른 모델의 입력 특성으로 활용(transform)할 수 있습니다.
* **단점:** 가장 큰 단점은 **K(클러스터 개수)** 값을 사전에 미리 지정해야 한다는 점입니다. 데이터에 몇 종류의 그룹이 있는지 미리 알 수 없을 때는 이 값을 정하기 어렵습니다.

**K값 결정 방법:**

* 완벽한 방법은 없지만, **엘보(Elbow) 방법**이 많이 사용됩니다. K값을 바꿔가며 K-평균을 여러 번 훈련한 후, **이너셔(inertia)** 값(클러스터 내부 응축도)의 변화를 그래프로 그립니다. 이때 이너셔 값이 급격히 줄어들다가 완만해지는 "팔꿈치"처럼 꺾이는 지점의 K값이 최적의 K값으로 간주됩니다.

**사이킷런(scikit-learn)에서 K-평균 사용:**

* `sklearn.cluster` 모듈 아래 `KMeans` 클래스로 제공됩니다.
* `KMeans` 객체를 만들 때 `n_clusters` 매개변수로 클러스터 개수(K)를 지정해야 합니다.
* `fit()` 메서드를 사용해 모델을 훈련하고, `labels_` 속성으로 각 샘플이 속한 클러스터 레이블을 확인할 수 있습니다.
* `cluster_centers_` 속성으로 각 클러스터의 중심점(평균)을 확인할 수 있습니다.

---
