## [혼자 공부하는 머신러닝+딥러닝] 6강. k-최근접 이웃 회귀 개정판 3장 1절 K-최근접 이웃 회기 요약

**주요 내용:**

* **회귀(Regression) 알고리즘 소개**: 분류가 정해진 클래스를 예측하는 반면, 회귀는 임의의 실수 값을 예측하는 문제입니다. 두 변수 사이의 상관관계를 분석하는 데 사용됩니다.
* **K-최근접 이웃 회귀**: K-최근접 이웃 알고리즘은 분류뿐만 아니라 회귀에도 사용될 수 있습니다. 예측하려는 샘플 주변의 K개 이웃 샘플들의 타깃 값(실수 값)의 평균을 내어 예측 값을 산출합니다.
* **농어 무게 예측 예시**: 농어의 길이 데이터만 사용하여 무게를 예측하는 예시를 통해 회귀 개념을 설명합니다. 농어의 길이와 무게 사이의 산점도를 통해 양의 상관관계를 시각적으로 확인합니다.
* **데이터 준비 및 모델 훈련**: 사이킷런의 `train_test_split` 함수를 사용하여 훈련 세트와 테스트 세트를 나누고, `KNeighborsRegressor` 클래스를 사용하여 K-최근접 이웃 회귀 모델을 훈련합니다. 입력 데이터는 2차원 배열로 변환해야 합니다.
* **모델 평가**: 회귀 모델의 성능은 일반적으로 R-제곱(결정 계수) 값으로 평가하며, 1에 가까울수록 좋은 모델입니다. 평균 절대 오차(Mean Absolute Error)를 통해 예측 값과 실제 값의 평균적인 차이를 측정할 수도 있습니다.
* **과소적합(Underfitting) 및 과대적합(Overfitting)**:
    * **과소적합**: 모델이 훈련 데이터를 충분히 학습하지 못하여 훈련 세트와 테스트 세트 모두에서 낮은 성능을 보이는 경우입니다.
    * **과대적합**: 모델이 훈련 데이터에 너무 과하게 맞춰져 훈련 세트에서는 높은 성능을 보이지만, 새로운 데이터인 테스트 세트에서는 낮은 성능을 보이는 경우입니다.
    * K-최근접 이웃 알고리즘에서는 이웃의 개수 K를 조절하여 과소적합 또는 과대적합 문제를 완화할 수 있습니다. K 값을 줄이면 과대적합 가능성이 높아지고, K 값을 늘리면 과소적합 가능성이 높아집니다.

**핵심 요약**: 회귀는 임의의 실수 값을 예측하는 것이며, K-최근접 이웃 알고리즘을 회귀에 적용할 수 있습니다. 모델의 성능 평가는 R-제곱 값 등을 사용하며, 과소적합과 과대적합을 이해하고 적절한 모델을 선택하는 것이 중요합니다.


----


## [혼자 공부하는 머신러닝+딥러닝] 7강. 사이킷런으로 선형 회귀 모델 만들어 보기 3장 2절 선형 회귀 요약

**주요 내용:**

* **회귀 모델 평가**: 회귀 모델은 정확도 대신 타깃 값과 예측값의 차이를 활용한 지표를 사용하며, 대표적인 것이 R-제곱(결정 계수)입니다. R-제곱 값은 0과 1 사이의 값을 가지며, 1에 가까울수록 좋은 모델입니다.
* **과소적합과 과대적합**: 훈련 세트와 테스트 세트의 성능 차이를 통해 모델의 과소적합 또는 과대적합 여부를 판단할 수 있습니다. K-최근접 이웃 회귀 알고리즘에서는 이웃의 개수 K를 조절하여 이를 완화할 수 있습니다.
* **K-최근접 이웃 회귀의 한계**: K-최근접 이웃 회귀는 훈련 세트 범위 밖의 값을 잘 예측하지 못하는 단점이 있습니다. 특히 농어 무게 예측 예시에서 긴 농어에 대한 예측이 제한되는 문제가 발생했습니다.
* **선형 회귀 소개**: 이러한 문제점을 해결하기 위해 선형 회귀 알고리즘을 소개합니다. 선형 회귀는 특성과 타깃 사이의 선형적인 관계를 모델링하며, 1차원 데이터에서는 직선의 방정식으로 표현됩니다.
* **사이킷런의 선형 회귀**: 사이킷런의 `linear_model.LinearRegression` 클래스를 사용하여 선형 회귀 모델을 훈련하고 예측할 수 있습니다. 모델은 학습된 계수(coefficient)와 절편(intercept)을 속성으로 가집니다.
* **선형 회귀 모델 평가**: 농어 무게 예측에 선형 회귀 모델을 적용한 결과, K-최근접 이웃 회귀보다 훈련 세트의 추세를 더 잘 따라가는 것을 확인했습니다. 하지만 테스트 세트 점수가 낮아 과소적합 가능성이 제기되었습니다. 또한, 선형 회귀 모델은 때때로 음수 무게와 같이 현실적으로 어색한 예측을 할 수 있다는 단점이 있습니다.
* **다항 회귀**: 선형 회귀의 단점을 보완하기 위해 다항 회귀를 소개합니다. 다항 회귀는 특성의 거듭제곱 항을 추가하여 비선형적인 관계를 모델링할 수 있습니다. 2차항을 추가한 다항 회귀 모델은 농어 무게 예측에서 더 높은 R-제곱 값을 보였고, 음수 예측 문제도 일부 개선할 수 있음을 시사했습니다.
* **다변수 선형 회귀**: 선형 회귀는 여러 개의 특성을 사용하여 더 복잡한 모델을 만들 수 있지만, 시각화의 어려움이 있습니다.
* **모델 평가의 중요성**: 모델이 과대적합 또는 과소적합되었는지 판단하고 적절한 모델을 선택하는 것은 중요한 과정이며, 여러 모델과 실험을 통해 상대적으로 평가해야 합니다. R-제곱 값의 '높다'는 기준은 문제의 특성에 따라 다를 수 있습니다.

**핵심 요약**: 선형 회귀는 K-최근접 이웃 회귀의 단점을 보완하며, 다항 회귀를 통해 더 복잡한 데이터 패턴을 모델링할 수 있습니다. 하지만 모델 평가와 과소적합/과대적합 방지를 위한 주의가 필요합니다.**
