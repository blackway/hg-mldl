
## [혼자 공부하는 머신러닝+딥러닝] 9강, 4장 1절 스터디 요약

혼공 머신러닝 딥러닝 개정판 4장 1절 강의는 **로지스틱 회귀**에 대한 내용입니다. 로지스틱 회귀는 머신러닝과 딥러닝의 핵심 기본 알고리즘으로, 이번 강의에서는 럭키백 판매 시나리오를 통해 확률 예측 문제에 어떻게 적용될 수 있는지 설명합니다.

**주요 내용:**

* **럭키백 시나리오:** 내용물을 알 수 없는 럭키백에서 특정 생선(도미, 빙어 등)이 나올 확률을 예측하는 상황을 가정합니다.
* **확률 예측:** 이전의 예측(클래스 분류 또는 임의의 숫자 예측)과는 달리, 0에서 1 사이의 확률 값으로 결과를 예측해야 합니다.
* **데이터:** 생선의 길이, 대각선 길이, 높이, 두께 등의 특징을 입력 데이터로 사용하며, 생선 종류를 타겟 데이터로 사용합니다.
* **K-최근접 이웃 알고리즘:** 이전 장에서 배운 K-최근접 이웃 알고리즘을 활용하여 이웃한 샘플들의 클래스 비율로 확률을 예측하는 방법을 간략히 복습합니다. 하지만 이 방법은 이웃 개수에 따라 제한적인 확률 값만 출력할 수 있다는 단점이 있습니다.
* **로지스틱 회귀:** 더 자연스러운 확률 결과를 얻기 위해 로지스틱 회귀를 소개합니다. 로지스틱 회귀는 대표적인 분류 알고리즘이며, 인공 신경망의 기본 단위라고 할 수 있을 정도로 중요한 개념입니다.
* **선형 방정식과 시그모이드 함수:** 로지스틱 회귀는 선형 회귀와 유사하게 선형 방정식을 학습하지만, 그 결과를 0과 1 사이의 확률 값으로 변환하기 위해 **시그모이드 함수 (로지스틱 함수)**를 사용합니다. 시그모이드 함수는 z 값(선형 방정식의 출력)을 0과 1 사이로 압축하는 역할을 합니다.
* **이진 분류:** 트레인 타겟에서 두 가지 클래스(브림, 스멜트)만 추출하여 로지스틱 회귀 모델을 훈련하고 예측하는 예시를 보여줍니다. 사이킷런은 문자열 타겟 데이터를 알파벳 순서로 정렬하여 클래스 레이블을 지정합니다.
* **결정 함수 (Decision Function):** 로지스틱 회귀 모델은 predict_proba 메소드를 통해 확률을 출력할 뿐만 아니라, decision_function 메소드를 통해 시그모이드 함수를 거치기 전의 z 값을 출력할 수 있습니다. 이 z 값의 부호만으로도 양성/음성 클래스를 예측할 수 있습니다.
* **다중 분류:** 로지스틱 회귀는 기본적으로 다중 분류를 지원합니다. 여러 개의 클래스를 가진 데이터를 사용하여 모델을 훈련하고, 각 클래스에 대한 확률을 예측하는 방법을 설명합니다. 다중 분류에서는 각 클래스마다 하나의 선형 방정식을 학습하는 것과 유사하게 동작합니다.
* **소프트맥스 함수:** 다중 분류에서 decision_function의 출력값(z 값)을 확률로 변환하기 위해 **소프트맥스 함수**를 사용합니다. 소프트맥스 함수는 시그모이드 함수를 다중 클래스 버전으로 확장한 개념으로 이해할 수 있습니다.

**핵심 요약:** 로지스틱 회귀는 선형 방정식을 기반으로 하지만, 시그모이드 함수 또는 소프트맥스 함수를 사용하여 0과 1 사이의 확률 값을 출력하는 강력한 분류 알고리즘입니다.
