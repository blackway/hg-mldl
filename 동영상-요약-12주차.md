## 혼자 공부하는 머신러닝 딥러닝 개정판 6장 3절 강의 요약

이번 강의는 **머신러닝 딥러닝 개정판 6장 3절 '주성분 분석(PCA)'**에 대한 내용입니다. PCA는 데이터를 줄이는 **차원 축소** 방법 중 하나로, 복잡한 데이터를 간단하게 만들면서도 중요한 정보를 대부분 유지하는 기술이에요.

---

### 차원 축소란?

* 우리가 흔히 사용하는 '차원'이라는 말은 배열의 축 개수를 의미하기도 하고, **벡터의 원소 개수**를 의미하기도 해요. 여기서 말하는 차원 축소는 그림이나 사진처럼 많은 픽셀(원본 특성)로 이루어진 데이터를 더 적은 수의 핵심적인 정보(특성)로 줄이는 것을 뜻합니다.
* 예를 들어, 10,000개의 픽셀로 표현되는 이미지를 50개의 특성으로 줄이는 것이 차원 축소입니다. 이렇게 하면 데이터를 저장하는 데 필요한 공간이 줄어들고, 나중에 모델을 훈련할 때 처리 속도도 빨라져요.

---

### 주성분 분석(PCA)의 원리

* PCA는 데이터가 가장 넓게 퍼져 있는 방향(주성분)을 찾아 그 방향으로 데이터를 **투영**시켜 차원을 줄입니다.
* **첫 번째 주성분**은 데이터의 분산(퍼져 있는 정도)이 가장 큰 방향을 찾아요.
* **두 번째 주성분**은 첫 번째 주성분과 수직이면서 남아있는 데이터의 분산이 가장 큰 방향을 찾습니다.
* 이런 식으로 필요한 만큼 주성분을 찾아내어 원본 데이터를 이 주성분들 위에 있는 점으로 변환하여 차원을 줄이는 거예요.

---

### 사이킷런(Scikit-learn)에서 PCA 사용하기

* **`PCA` 클래스**를 사용해서 PCA를 구현할 수 있어요.
* `n_components` 매개변수에 원하는 주성분 개수를 지정합니다 (예: 50개). 이 값을 0부터 1 사이의 실수로 주면, 전체 분산 중 얼마를 유지할지 비율로 지정할 수도 있어요 (예: 0.95는 95%의 분산 유지).
* `fit()` 메서드로 데이터를 훈련시키고, `transform()` 메서드로 실제 데이터를 변환하여 차원을 줄입니다.
* `components_` 속성으로 PCA가 찾아낸 주성분들을 확인할 수 있어요.
* `inverse_transform()` 메서드를 사용하면 줄어든 차원의 데이터를 다시 원래 차원으로 복원할 수 있습니다. 이때 원본 이미지와 거의 비슷하게 복원되는 것을 볼 수 있는데, 이는 PCA가 중요한 정보를 잘 보존하고 있다는 의미예요.
* `explained_variance_ratio_` 속성을 통해 각 주성분이 전체 분산을 얼마나 잘 설명하는지 확인할 수 있습니다. 주성분 개수가 적어도 전체 분산의 상당 부분을 설명할 수 있다는 것을 알 수 있어요 (예: 50개의 주성분으로 92%의 분산 설명).

---

### PCA의 장점

* **저장 공간 감소**: 데이터의 차원을 줄여 저장 공간을 절약할 수 있어요.
* **모델 훈련 속도 향상**: 특성(차원)이 줄어들면 모델 훈련 시간이 크게 단축됩니다. 예를 들어, 10,000개의 특성을 가진 데이터를 50개로 줄이면 훈련 속도가 약 37배 정도 빨라질 수 있어요.
* **성능 유지 또는 향상**: 차원을 줄여도 모델의 성능은 크게 저하되지 않거나 오히려 더 좋아질 수 있어요.
* **시각화 용이**: 복잡한 고차원 데이터를 2차원이나 3차원으로 줄여 시각적으로 쉽게 이해하고 분석할 수 있게 해줍니다.
* **다른 모델과의 결합**: PCA로 차원 축소된 데이터를 분류(예: 로지스틱 회귀)나 군집화(예: K-평균) 같은 다른 머신러닝 알고리즘에 활용할 수 있으며, 좋은 성능을 얻을 수 있습니다.

이번 강의에서는 차원 축소의 대표적인 방법인 주성분 분석에 대해 자세히 알아보았습니다. 다음 시간에는 딥러닝에 대해 배우기 시작할 거예요.
