## [혼자 공부하는 머신러닝+딥러닝] 8강, 3장 3절 특성 공학과 규제 스터디 요약

이번 강의에서는 **다중 회귀**, **특성 공학**, 그리고 모델의 **과대적합을 방지하기 위한 규제**에 대해 다루었습니다.

**주요 내용:**

* **다중 회귀:** 여러 개의 특성을 사용하여 회귀 모델을 훈련하는 방법입니다. 시각화는 어렵지만, 평가 지표를 통해 모델 성능을 확인해야 합니다.
* **특성 공학:** 기존 특성을 제곱하거나 곱하는 등의 연산을 통해 새로운 특성을 만들어 모델의 성능을 향상시키는 기법입니다. 사이킷런의 `PolynomialFeatures` 클래스를 사용하여 다항 특성을 쉽게 생성할 수 있습니다. 이 클래스는 주어진 특성을 조합하여 더 높은 차수의 특성을 자동으로 만들어줍니다.
* **변환기 (Transformer):** 사이킷런에서 데이터를 변환하는 클래스를 의미하며, `fit` 메서드로 데이터를 학습하고 `transform` 메서드로 실제 변환을 수행합니다. `PolynomialFeatures`는 대표적인 변환기입니다.
* **특성 스케일링 (표준화):** 규제를 적용하기 전에 특성들의 스케일을 비슷하게 맞춰주는 것이 중요합니다. 표준화는 평균을 0, 표준편차를 1로 만드는 대표적인 스케일링 방법이며, 사이킷런의 `StandardScaler` 클래스를 사용합니다.
* **규제 (Regularization):** 모델이 훈련 데이터에 너무 과하게 학습하여 과대적합되는 것을 막는 방법입니다. 선형 회귀 모델에서는 계수(기울기) 값을 작게 만드는 방식으로 규제를 적용합니다.
    * **릿지 회귀 (L2 규제):** 가중치의 제곱을 벌칙으로 사용하여 큰 가중치를 줄이는 방식입니다. 사이킷런의 `Ridge` 클래스를 사용합니다. 알파(alpha) 매개변수로 규제의 강도를 조절합니다.
    * **라쏘 회귀 (L1 규제):** 가중치의 절댓값을 벌칙으로 사용하여 일부 가중치를 완전히 0으로 만들 수도 있습니다. 사이킷런의 `Lasso` 클래스를 사용합니다. 알파 매개변수로 규제의 강도를 조절합니다.
* **하이퍼파라미터:** 모델이 훈련 데이터에서 학습할 수 없는 값으로, 사용자가 실험을 통해 최적의 값을 찾아야 하는 매개변수입니다 (예: 릿지와 라쏘의 알파 값). 다양한 방법을 통해 최적의 하이퍼파라미터를 탐색할 수 있습니다.

**핵심 요약:**

* 다중 특성과 다항 특성을 사용하여 선형 회귀 모델의 훈련 세트 성능을 크게 향상시킬 수 있었습니다.
* 하지만 특성을 너무 많이 추가하면 과대적합이 심해져 테스트 세트 성능이 저하될 수 있습니다.
* 릿지 및 라쏘 회귀를 통해 과대적합을 효과적으로 방지하고, 훈련 세트와 테스트 세트 간의 성능 차이를 줄일 수 있었습니다.
* 라쏘 회귀는 일부 특성의 가중치를 0으로 만들어 특성 선택의 효과를 보이기도 했습니다.
* 실무에서는 L1 규제인 라쏘보다는 L2 규제인 릿지 모델이 더 선호되는 경향이 있습니다.

**결론 : 적절한 특성 공학과 규제 기법을 통해 머신러닝 모델의 성능을 향상시키고 과대적합을 방지하는 것이 중요합니다.**
